const { db } = require("./database/db");

/*
db.query('SELECT * FROM colonne', (err, results) => {
    if (err) {
      console.error('Erreur de requête : ', err);
      return;
    }
    console.log('Résultats de la requête : ', results);
  });
*/
